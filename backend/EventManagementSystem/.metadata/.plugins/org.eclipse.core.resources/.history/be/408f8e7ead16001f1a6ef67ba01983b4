package com.eventmangementsystem.controller;

import com.eventmangementsystem.entity.Offer;
import com.eventmangementsystem.service.DiscountService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/offers")
public class OfferController {

    @Autowired
    private DiscountService discountService;

    @GetMapping
    public List<Offer> getAllOffers() {
        return discountService.getAllOffers();
    }

    @GetMapping("/{id}")
    public ResponseEntity<Offer> getOfferById(@PathVariable int id) {
        Offer offer = discountService.getOfferById(id);
        if (offer == null) {
            return ResponseEntity.notFound().build();
        }
        return ResponseEntity.ok(offer);
    }

    @PostMapping("/add")
    public Offer createOffer(@RequestBody Offer offer) {
        return discountService.createOffer(offer);
    }

    @PutMapping("/update/{id}")
    public ResponseEntity<Offer> updateOffer(@PathVariable int id, @RequestBody Offer offerDetails) {
        Offer offer = discountService.getOfferById(id);
        if (offer == null) {
            return ResponseEntity.notFound().build();
        }
        offer.setOfferName(offerDetails.getOfferName());
        offer.setOfferPercentage(offerDetails.getOfferPercentage());
        offer.setOfferEligibility(offerDetails.getOfferEligibility());
        offer.setStartDate(offerDetails.getStartDate());
        offer.setEndDate(offerDetails.getEndDate());

        Offer updatedOffer = discountService.updateOffer(offer);
        return ResponseEntity.ok(updatedOffer);
    }

    @DeleteMapping("/delete/{id}")
    public ResponseEntity<Void> deleteOffer(@PathVariable int id) {
        Offer offer = discountService.getOfferById(id);
        if (offer == null) {
            return ResponseEntity.notFound().build();
        }
        discountService.deleteOffer(id);
        return ResponseEntity.noContent().build();
    }
}
